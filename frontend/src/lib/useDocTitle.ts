/**
 * useDocTitle — intentionally a no-op.
 * The static title "Learnexa — SSB International School" is set in index.html
 * and should not be overridden per-page.
 *
 * Kept as a stub so existing call-sites don't need to be removed.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function useDocTitle(_title?: string) {
  // no-op — static title from index.html is used
}
